# Name of the postgres database
export APP_NAME=todos-electrified

# Docker images
export POSTGRESQL_IMAGE=postgres:14-alpine

export ELECTRIC_IMAGE=electricsql/electric:0.6.3
# Use the following if you want to test the Electric service on a non stable version
#export ELECTRIC_IMAGE=electric:local-build